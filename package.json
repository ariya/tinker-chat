{
  "name": "tinker-chat",
  "version": "0.0.1",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "npm run nbb main.cljs",
    "nbb": "nbb",
    "nrepl": "nbb nrepl-server :port 9000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "nbb": "~1.2.174"
  }
}
